#include "main.h"
#include <stdio.h>

/**
 * print_numbers - print numbers 0-9 followed by new line
 * Description: function to print numbers 0-9 followed by a new line
 * Result: Always 0
 */
void print_numbers(void)
{
int n;
for (n = 0; n <= 9; n++)
{
_putchar(n);
}
_putchar('\n');
}
